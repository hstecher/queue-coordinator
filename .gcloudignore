# This file specifies files that should NOT be uploaded to Google Cloud
# using gcloud. It follows the same syntax as .gitignore, with the addition of
# "#!include" directives (which insert rules from other files) and "#!-" directives
# (which suppress rules from the gicloudignore file generated by gcloud).

node_modules/
npm-debug.log
.git
.gitignore
.env
.DS_Store
